# Use the README file as a reference to customize settings.

DailyMenu:
  fields:
    default: date, breakfast, lunch, dinner
    form: date, breakfast, lunch, dinner
  relationships: 
  application: Application
