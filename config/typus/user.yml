# Use the README file as a reference to customize settings.

User:
  fields:
    default: id, email, active, activation_code, activation_start, created_at
    form: email, active
    list: id, email, active
  relationships: 
  application: Application
