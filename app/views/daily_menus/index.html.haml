.page-header
  %h1 献立表
%p= link_to '献立を追加', new_daily_menu_path, class: 'btn primary'
%table
  - @daily_menus.each_slice(7) do |row|
    %tr
      - row.each do |day|
        %td
          %h5= link_to l(day.date, format: :short), day
