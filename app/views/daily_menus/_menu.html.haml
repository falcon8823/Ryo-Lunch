- if menu
  %ul{data: {role: :listview, inset: :true}}
    - if date_str
      %li{data:{role: 'list-divider'}}
        %h2
          -#= "今日(#{l Date.today, format: :very_short})"
          = date_str
          のメニュー
    %li
      朝食
      %ul
        - menu.breakfast.each_line do |i|
          %li= i
    %li
      昼食
      %ul
        - menu.lunch.each_line do |i|
          %li= i
    %li
      夕食
      %ul
        - menu.dinner.each_line do |i|
          %li= i
- else
  %h3 献立データが登録されていません。
